export interface IProduct {
  id: string;
  title: string;
  image: string;
  description: string;
  price: number;
}

export interface IBanner {
  id: string;
  title: string;
  description: string;
  image: string;
}
